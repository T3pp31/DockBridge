// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

// swiftlint:disable all
import Foundation

// Depending on the consumer's build setup, the low-level FFI code
// might be in a separate module, or it might be compiled inline into
// this module. This is a bit of light hackery to work with both.
#if canImport(dockbridge_uniffiFFI)
import dockbridge_uniffiFFI
#endif

fileprivate extension RustBuffer {
    // Allocate a new buffer, copying the contents of a `UInt8` array.
    init(bytes: [UInt8]) {
        let rbuf = bytes.withUnsafeBufferPointer { ptr in
            RustBuffer.from(ptr)
        }
        self.init(capacity: rbuf.capacity, len: rbuf.len, data: rbuf.data)
    }

    static func empty() -> RustBuffer {
        RustBuffer(capacity: 0, len:0, data: nil)
    }

    static func from(_ ptr: UnsafeBufferPointer<UInt8>) -> RustBuffer {
        try! rustCall { ffi_dockbridge_uniffi_rustbuffer_from_bytes(ForeignBytes(bufferPointer: ptr), $0) }
    }

    // Frees the buffer in place.
    // The buffer must not be used after this is called.
    func deallocate() {
        try! rustCall { ffi_dockbridge_uniffi_rustbuffer_free(self, $0) }
    }
}

fileprivate extension ForeignBytes {
    init(bufferPointer: UnsafeBufferPointer<UInt8>) {
        self.init(len: Int32(bufferPointer.count), data: bufferPointer.baseAddress)
    }
}

// For every type used in the interface, we provide helper methods for conveniently
// lifting and lowering that type from C-compatible data, and for reading and writing
// values of that type in a buffer.

// Helper classes/extensions that don't change.
// Someday, this will be in a library of its own.

fileprivate extension Data {
    init(rustBuffer: RustBuffer) {
        self.init(
            bytesNoCopy: rustBuffer.data!,
            count: Int(rustBuffer.len),
            deallocator: .none
        )
    }
}

// Define reader functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.
//
// With external types, one swift source file needs to be able to call the read
// method on another source file's FfiConverter, but then what visibility
// should Reader have?
// - If Reader is fileprivate, then this means the read() must also
//   be fileprivate, which doesn't work with external types.
// - If Reader is internal/public, we'll get compile errors since both source
//   files will try define the same type.
//
// Instead, the read() method and these helper functions input a tuple of data

fileprivate func createReader(data: Data) -> (data: Data, offset: Data.Index) {
    (data: data, offset: 0)
}

// Reads an integer at the current offset, in big-endian order, and advances
// the offset on success. Throws if reading the integer would move the
// offset past the end of the buffer.
fileprivate func readInt<T: FixedWidthInteger>(_ reader: inout (data: Data, offset: Data.Index)) throws -> T {
    let range = reader.offset..<reader.offset + MemoryLayout<T>.size
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    if T.self == UInt8.self {
        let value = reader.data[reader.offset]
        reader.offset += 1
        return value as! T
    }
    var value: T = 0
    let _ = withUnsafeMutableBytes(of: &value, { reader.data.copyBytes(to: $0, from: range)})
    reader.offset = range.upperBound
    return value.bigEndian
}

// Reads an arbitrary number of bytes, to be used to read
// raw bytes, this is useful when lifting strings
fileprivate func readBytes(_ reader: inout (data: Data, offset: Data.Index), count: Int) throws -> Array<UInt8> {
    let range = reader.offset..<(reader.offset+count)
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    var value = [UInt8](repeating: 0, count: count)
    value.withUnsafeMutableBufferPointer({ buffer in
        reader.data.copyBytes(to: buffer, from: range)
    })
    reader.offset = range.upperBound
    return value
}

// Reads a float at the current offset.
fileprivate func readFloat(_ reader: inout (data: Data, offset: Data.Index)) throws -> Float {
    return Float(bitPattern: try readInt(&reader))
}

// Reads a float at the current offset.
fileprivate func readDouble(_ reader: inout (data: Data, offset: Data.Index)) throws -> Double {
    return Double(bitPattern: try readInt(&reader))
}

// Indicates if the offset has reached the end of the buffer.
fileprivate func hasRemaining(_ reader: (data: Data, offset: Data.Index)) -> Bool {
    return reader.offset < reader.data.count
}

// Define writer functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.  See the above discussion on Readers for details.

fileprivate func createWriter() -> [UInt8] {
    return []
}

fileprivate func writeBytes<S>(_ writer: inout [UInt8], _ byteArr: S) where S: Sequence, S.Element == UInt8 {
    writer.append(contentsOf: byteArr)
}

// Writes an integer in big-endian order.
//
// Warning: make sure what you are trying to write
// is in the correct type!
fileprivate func writeInt<T: FixedWidthInteger>(_ writer: inout [UInt8], _ value: T) {
    var value = value.bigEndian
    withUnsafeBytes(of: &value) { writer.append(contentsOf: $0) }
}

fileprivate func writeFloat(_ writer: inout [UInt8], _ value: Float) {
    writeInt(&writer, value.bitPattern)
}

fileprivate func writeDouble(_ writer: inout [UInt8], _ value: Double) {
    writeInt(&writer, value.bitPattern)
}

// Protocol for types that transfer other types across the FFI. This is
// analogous to the Rust trait of the same name.
fileprivate protocol FfiConverter {
    associatedtype FfiType
    associatedtype SwiftType

    static func lift(_ value: FfiType) throws -> SwiftType
    static func lower(_ value: SwiftType) -> FfiType
    static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType
    static func write(_ value: SwiftType, into buf: inout [UInt8])
}

// Types conforming to `Primitive` pass themselves directly over the FFI.
fileprivate protocol FfiConverterPrimitive: FfiConverter where FfiType == SwiftType { }

extension FfiConverterPrimitive {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ value: FfiType) throws -> SwiftType {
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> FfiType {
        return value
    }
}

// Types conforming to `FfiConverterRustBuffer` lift and lower into a `RustBuffer`.
// Used for complex types where it's hard to write a custom lift/lower.
fileprivate protocol FfiConverterRustBuffer: FfiConverter where FfiType == RustBuffer {}

extension FfiConverterRustBuffer {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ buf: RustBuffer) throws -> SwiftType {
        var reader = createReader(data: Data(rustBuffer: buf))
        let value = try read(from: &reader)
        if hasRemaining(reader) {
            throw UniffiInternalError.incompleteData
        }
        buf.deallocate()
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> RustBuffer {
          var writer = createWriter()
          write(value, into: &writer)
          return RustBuffer(bytes: writer)
    }
}
// An error type for FFI errors. These errors occur at the UniFFI level, not
// the library level.
fileprivate enum UniffiInternalError: LocalizedError {
    case bufferOverflow
    case incompleteData
    case unexpectedOptionalTag
    case unexpectedEnumCase
    case unexpectedNullPointer
    case unexpectedRustCallStatusCode
    case unexpectedRustCallError
    case unexpectedStaleHandle
    case rustPanic(_ message: String)

    public var errorDescription: String? {
        switch self {
        case .bufferOverflow: return "Reading the requested value would read past the end of the buffer"
        case .incompleteData: return "The buffer still has data after lifting its containing value"
        case .unexpectedOptionalTag: return "Unexpected optional tag; should be 0 or 1"
        case .unexpectedEnumCase: return "Raw enum value doesn't match any cases"
        case .unexpectedNullPointer: return "Raw pointer value was null"
        case .unexpectedRustCallStatusCode: return "Unexpected RustCallStatus code"
        case .unexpectedRustCallError: return "CALL_ERROR but no errorClass specified"
        case .unexpectedStaleHandle: return "The object in the handle map has been dropped already"
        case let .rustPanic(message): return message
        }
    }
}

fileprivate extension NSLock {
    func withLock<T>(f: () throws -> T) rethrows -> T {
        self.lock()
        defer { self.unlock() }
        return try f()
    }
}

fileprivate let CALL_SUCCESS: Int8 = 0
fileprivate let CALL_ERROR: Int8 = 1
fileprivate let CALL_UNEXPECTED_ERROR: Int8 = 2
fileprivate let CALL_CANCELLED: Int8 = 3

fileprivate extension RustCallStatus {
    init() {
        self.init(
            code: CALL_SUCCESS,
            errorBuf: RustBuffer.init(
                capacity: 0,
                len: 0,
                data: nil
            )
        )
    }
}

private func rustCall<T>(_ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    let neverThrow: ((RustBuffer) throws -> Never)? = nil
    return try makeRustCall(callback, errorHandler: neverThrow)
}

private func rustCallWithError<T, E: Swift.Error>(
    _ errorHandler: @escaping (RustBuffer) throws -> E,
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    try makeRustCall(callback, errorHandler: errorHandler)
}

private func makeRustCall<T, E: Swift.Error>(
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T,
    errorHandler: ((RustBuffer) throws -> E)?
) throws -> T {
    uniffiEnsureDockbridgeUniffiInitialized()
    var callStatus = RustCallStatus.init()
    let returnedVal = callback(&callStatus)
    try uniffiCheckCallStatus(callStatus: callStatus, errorHandler: errorHandler)
    return returnedVal
}

private func uniffiCheckCallStatus<E: Swift.Error>(
    callStatus: RustCallStatus,
    errorHandler: ((RustBuffer) throws -> E)?
) throws {
    switch callStatus.code {
        case CALL_SUCCESS:
            return

        case CALL_ERROR:
            if let errorHandler = errorHandler {
                throw try errorHandler(callStatus.errorBuf)
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.unexpectedRustCallError
            }

        case CALL_UNEXPECTED_ERROR:
            // When the rust code sees a panic, it tries to construct a RustBuffer
            // with the message.  But if that code panics, then it just sends back
            // an empty buffer.
            if callStatus.errorBuf.len > 0 {
                throw UniffiInternalError.rustPanic(try FfiConverterString.lift(callStatus.errorBuf))
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.rustPanic("Rust panic")
            }

        case CALL_CANCELLED:
            fatalError("Cancellation not supported yet")

        default:
            throw UniffiInternalError.unexpectedRustCallStatusCode
    }
}

private func uniffiTraitInterfaceCall<T>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> ()
) {
    do {
        try writeReturn(makeCall())
    } catch let error {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}

private func uniffiTraitInterfaceCallWithError<T, E>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> (),
    lowerError: (E) -> RustBuffer
) {
    do {
        try writeReturn(makeCall())
    } catch let error as E {
        callStatus.pointee.code = CALL_ERROR
        callStatus.pointee.errorBuf = lowerError(error)
    } catch {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}
// Initial value and increment amount for handles. 
// These ensure that SWIFT handles always have the lowest bit set
fileprivate let UNIFFI_HANDLEMAP_INITIAL: UInt64 = 1
fileprivate let UNIFFI_HANDLEMAP_DELTA: UInt64 = 2

fileprivate final class UniffiHandleMap<T>: @unchecked Sendable {
    // All mutation happens with this lock held, which is why we implement @unchecked Sendable.
    private let lock = NSLock()
    private var map: [UInt64: T] = [:]
    private var currentHandle: UInt64 = UNIFFI_HANDLEMAP_INITIAL

    func insert(obj: T) -> UInt64 {
        lock.withLock {
            return doInsert(obj)
        }
    }

    // Low-level insert function, this assumes `lock` is held.
    private func doInsert(_ obj: T) -> UInt64 {
        let handle = currentHandle
        currentHandle += UNIFFI_HANDLEMAP_DELTA
        map[handle] = obj
        return handle
    }

     func get(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

     func clone(handle: UInt64) throws -> UInt64 {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return doInsert(obj)
        }
    }

    @discardableResult
    func remove(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map.removeValue(forKey: handle) else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    var count: Int {
        get {
            map.count
        }
    }
}


// Public interface members begin here.
// Magic number for the Rust proxy to call using the same mechanism as every other method,
// to free the callback once it's dropped by Rust.
private let IDX_CALLBACK_FREE: Int32 = 0
// Callback return codes
private let UNIFFI_CALLBACK_SUCCESS: Int32 = 0
private let UNIFFI_CALLBACK_ERROR: Int32 = 1
private let UNIFFI_CALLBACK_UNEXPECTED_ERROR: Int32 = 2

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt16: FfiConverterPrimitive {
    typealias FfiType = UInt16
    typealias SwiftType = UInt16

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt16 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt32: FfiConverterPrimitive {
    typealias FfiType = UInt32
    typealias SwiftType = UInt32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt64: FfiConverterPrimitive {
    typealias FfiType = UInt64
    typealias SwiftType = UInt64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterBool : FfiConverter {
    typealias FfiType = Int8
    typealias SwiftType = Bool

    public static func lift(_ value: Int8) throws -> Bool {
        return value != 0
    }

    public static func lower(_ value: Bool) -> Int8 {
        return value ? 1 : 0
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bool {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Bool, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterString: FfiConverter {
    typealias SwiftType = String
    typealias FfiType = RustBuffer

    public static func lift(_ value: RustBuffer) throws -> String {
        defer {
            value.deallocate()
        }
        if value.data == nil {
            return String()
        }
        let bytes = UnsafeBufferPointer<UInt8>(start: value.data!, count: Int(value.len))
        return String(bytes: bytes, encoding: String.Encoding.utf8)!
    }

    public static func lower(_ value: String) -> RustBuffer {
        return value.utf8CString.withUnsafeBufferPointer { ptr in
            // The swift string gives us int8_t, we want uint8_t.
            ptr.withMemoryRebound(to: UInt8.self) { ptr in
                // The swift string gives us a trailing null byte, we don't want it.
                let buf = UnsafeBufferPointer(rebasing: ptr.prefix(upTo: ptr.count - 1))
                return RustBuffer.from(buf)
            }
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> String {
        let len: Int32 = try readInt(&buf)
        return String(bytes: try readBytes(&buf, count: Int(len)), encoding: String.Encoding.utf8)!
    }

    public static func write(_ value: String, into buf: inout [UInt8]) {
        let len = Int32(value.utf8.count)
        writeInt(&buf, len)
        writeBytes(&buf, value.utf8)
    }
}




/**
 * Main DockBridge client exposed to Swift.
 */
public protocol DockBridgeClientProtocol: AnyObject, Sendable {
    
    func cancelTransfer(taskId: UInt64) throws 
    
    func connect(profile: ConnectionProfileRecord) throws  -> UInt64
    
    func createDirectory(sessionId: UInt64, remotePath: String) throws 
    
    func delete(sessionId: UInt64, remotePath: String) throws 
    
    func disconnect(sessionId: UInt64) throws 
    
    func download(sessionId: UInt64, remotePath: String, localPath: String) throws 
    
    func downloadEntry(sessionId: UInt64, remotePath: String, localDirectory: String) throws 
    
    func getInitialDirectory(sessionId: UInt64) throws  -> String
    
    func getTransferQueue()  -> [TransferTaskRecord]
    
    func listDirectory(sessionId: UInt64, path: String) throws  -> [RemoteFileRecord]
    
    func rename(sessionId: UInt64, from: String, to: String) throws 
    
    func upload(sessionId: UInt64, localPath: String, remotePath: String) throws 
    
    func uploadEntry(sessionId: UInt64, localPath: String, remoteDirectory: String) throws 
    
}
/**
 * Main DockBridge client exposed to Swift.
 */
open class DockBridgeClient: DockBridgeClientProtocol, @unchecked Sendable {
    fileprivate let handle: UInt64

    /// Used to instantiate a [FFIObject] without an actual handle, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoHandle {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromHandle handle: UInt64) {
        self.handle = handle
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noHandle: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing handle the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noHandle: NoHandle) {
        self.handle = 0
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiCloneHandle() -> UInt64 {
        return try! rustCall { uniffi_dockbridge_uniffi_fn_clone_dockbridgeclient(self.handle, $0) }
    }
public convenience init(appConfig: AppConfigRecord, hostKeyHandler: HostKeyHandler, connectionEventHandler: ConnectionEventHandler)throws  {
    let handle =
        try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_constructor_dockbridgeclient_new(
        FfiConverterTypeAppConfigRecord_lower(appConfig),
        FfiConverterCallbackInterfaceHostKeyHandler_lower(hostKeyHandler),
        FfiConverterCallbackInterfaceConnectionEventHandler_lower(connectionEventHandler),$0
    )
}
    self.init(unsafeFromHandle: handle)
}

    deinit {
        if handle == 0 {
            // Mock objects have handle=0 don't try to free them
            return
        }

        try! rustCall { uniffi_dockbridge_uniffi_fn_free_dockbridgeclient(handle, $0) }
    }

    

    
open func cancelTransfer(taskId: UInt64)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_cancel_transfer(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(taskId),$0
    )
}
}
    
open func connect(profile: ConnectionProfileRecord)throws  -> UInt64  {
    return try  FfiConverterUInt64.lift(try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_connect(
            self.uniffiCloneHandle(),
        FfiConverterTypeConnectionProfileRecord_lower(profile),$0
    )
})
}
    
open func createDirectory(sessionId: UInt64, remotePath: String)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_create_directory(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(remotePath),$0
    )
}
}
    
open func delete(sessionId: UInt64, remotePath: String)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_delete(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(remotePath),$0
    )
}
}
    
open func disconnect(sessionId: UInt64)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_disconnect(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),$0
    )
}
}
    
open func download(sessionId: UInt64, remotePath: String, localPath: String)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_download(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(remotePath),
        FfiConverterString.lower(localPath),$0
    )
}
}
    
open func downloadEntry(sessionId: UInt64, remotePath: String, localDirectory: String)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_download_entry(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(remotePath),
        FfiConverterString.lower(localDirectory),$0
    )
}
}
    
open func getInitialDirectory(sessionId: UInt64)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_get_initial_directory(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),$0
    )
})
}
    
open func getTransferQueue() -> [TransferTaskRecord]  {
    return try!  FfiConverterSequenceTypeTransferTaskRecord.lift(try! rustCall() {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_get_transfer_queue(
            self.uniffiCloneHandle(),$0
    )
})
}
    
open func listDirectory(sessionId: UInt64, path: String)throws  -> [RemoteFileRecord]  {
    return try  FfiConverterSequenceTypeRemoteFileRecord.lift(try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_list_directory(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(path),$0
    )
})
}
    
open func rename(sessionId: UInt64, from: String, to: String)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_rename(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(from),
        FfiConverterString.lower(to),$0
    )
}
}
    
open func upload(sessionId: UInt64, localPath: String, remotePath: String)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_upload(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(localPath),
        FfiConverterString.lower(remotePath),$0
    )
}
}
    
open func uploadEntry(sessionId: UInt64, localPath: String, remoteDirectory: String)throws   {try rustCallWithError(FfiConverterTypeDockBridgeError_lift) {
    uniffi_dockbridge_uniffi_fn_method_dockbridgeclient_upload_entry(
            self.uniffiCloneHandle(),
        FfiConverterUInt64.lower(sessionId),
        FfiConverterString.lower(localPath),
        FfiConverterString.lower(remoteDirectory),$0
    )
}
}
    

    
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDockBridgeClient: FfiConverter {
    typealias FfiType = UInt64
    typealias SwiftType = DockBridgeClient

    public static func lift(_ handle: UInt64) throws -> DockBridgeClient {
        return DockBridgeClient(unsafeFromHandle: handle)
    }

    public static func lower(_ value: DockBridgeClient) -> UInt64 {
        return value.uniffiCloneHandle()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DockBridgeClient {
        let handle: UInt64 = try readInt(&buf)
        return try lift(handle)
    }

    public static func write(_ value: DockBridgeClient, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDockBridgeClient_lift(_ handle: UInt64) throws -> DockBridgeClient {
    return try FfiConverterTypeDockBridgeClient.lift(handle)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDockBridgeClient_lower(_ value: DockBridgeClient) -> UInt64 {
    return FfiConverterTypeDockBridgeClient.lower(value)
}




/**
 * Application configuration passed from Swift.
 */
public struct AppConfigRecord: Equatable, Hashable {
    public var connectionTimeoutSecs: UInt64
    public var sessionHealthCheckIntervalSecs: UInt64
    public var transferRetryCount: UInt32
    public var transferChunkSizeBytes: UInt64
    public var knownHostsPath: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(connectionTimeoutSecs: UInt64, sessionHealthCheckIntervalSecs: UInt64, transferRetryCount: UInt32, transferChunkSizeBytes: UInt64, knownHostsPath: String) {
        self.connectionTimeoutSecs = connectionTimeoutSecs
        self.sessionHealthCheckIntervalSecs = sessionHealthCheckIntervalSecs
        self.transferRetryCount = transferRetryCount
        self.transferChunkSizeBytes = transferChunkSizeBytes
        self.knownHostsPath = knownHostsPath
    }

    

    
}

#if compiler(>=6)
extension AppConfigRecord: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAppConfigRecord: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AppConfigRecord {
        return
            try AppConfigRecord(
                connectionTimeoutSecs: FfiConverterUInt64.read(from: &buf), 
                sessionHealthCheckIntervalSecs: FfiConverterUInt64.read(from: &buf), 
                transferRetryCount: FfiConverterUInt32.read(from: &buf), 
                transferChunkSizeBytes: FfiConverterUInt64.read(from: &buf), 
                knownHostsPath: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: AppConfigRecord, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.connectionTimeoutSecs, into: &buf)
        FfiConverterUInt64.write(value.sessionHealthCheckIntervalSecs, into: &buf)
        FfiConverterUInt32.write(value.transferRetryCount, into: &buf)
        FfiConverterUInt64.write(value.transferChunkSizeBytes, into: &buf)
        FfiConverterString.write(value.knownHostsPath, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAppConfigRecord_lift(_ buf: RustBuffer) throws -> AppConfigRecord {
    return try FfiConverterTypeAppConfigRecord.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAppConfigRecord_lower(_ value: AppConfigRecord) -> RustBuffer {
    return FfiConverterTypeAppConfigRecord.lower(value)
}


/**
 * SSH connection parameters passed from Swift.
 */
public struct ConnectionProfileRecord: Equatable, Hashable {
    public var host: String
    public var port: UInt16
    public var username: String
    public var authType: AuthTypeRecord

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(host: String, port: UInt16, username: String, authType: AuthTypeRecord) {
        self.host = host
        self.port = port
        self.username = username
        self.authType = authType
    }

    

    
}

#if compiler(>=6)
extension ConnectionProfileRecord: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeConnectionProfileRecord: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ConnectionProfileRecord {
        return
            try ConnectionProfileRecord(
                host: FfiConverterString.read(from: &buf), 
                port: FfiConverterUInt16.read(from: &buf), 
                username: FfiConverterString.read(from: &buf), 
                authType: FfiConverterTypeAuthTypeRecord.read(from: &buf)
        )
    }

    public static func write(_ value: ConnectionProfileRecord, into buf: inout [UInt8]) {
        FfiConverterString.write(value.host, into: &buf)
        FfiConverterUInt16.write(value.port, into: &buf)
        FfiConverterString.write(value.username, into: &buf)
        FfiConverterTypeAuthTypeRecord.write(value.authType, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeConnectionProfileRecord_lift(_ buf: RustBuffer) throws -> ConnectionProfileRecord {
    return try FfiConverterTypeConnectionProfileRecord.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeConnectionProfileRecord_lower(_ value: ConnectionProfileRecord) -> RustBuffer {
    return FfiConverterTypeConnectionProfileRecord.lower(value)
}


/**
 * Host key verification challenge presented to Swift.
 */
public struct HostKeyChallenge: Equatable, Hashable {
    public var host: String
    public var port: UInt16
    public var fingerprintSha256: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(host: String, port: UInt16, fingerprintSha256: String) {
        self.host = host
        self.port = port
        self.fingerprintSha256 = fingerprintSha256
    }

    

    
}

#if compiler(>=6)
extension HostKeyChallenge: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeHostKeyChallenge: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> HostKeyChallenge {
        return
            try HostKeyChallenge(
                host: FfiConverterString.read(from: &buf), 
                port: FfiConverterUInt16.read(from: &buf), 
                fingerprintSha256: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: HostKeyChallenge, into buf: inout [UInt8]) {
        FfiConverterString.write(value.host, into: &buf)
        FfiConverterUInt16.write(value.port, into: &buf)
        FfiConverterString.write(value.fingerprintSha256, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeHostKeyChallenge_lift(_ buf: RustBuffer) throws -> HostKeyChallenge {
    return try FfiConverterTypeHostKeyChallenge.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeHostKeyChallenge_lower(_ value: HostKeyChallenge) -> RustBuffer {
    return FfiConverterTypeHostKeyChallenge.lower(value)
}


/**
 * Remote file metadata returned to Swift.
 */
public struct RemoteFileRecord: Equatable, Hashable {
    public var name: String
    public var path: String
    public var isDirectory: Bool
    public var size: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(name: String, path: String, isDirectory: Bool, size: UInt64) {
        self.name = name
        self.path = path
        self.isDirectory = isDirectory
        self.size = size
    }

    

    
}

#if compiler(>=6)
extension RemoteFileRecord: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeRemoteFileRecord: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> RemoteFileRecord {
        return
            try RemoteFileRecord(
                name: FfiConverterString.read(from: &buf), 
                path: FfiConverterString.read(from: &buf), 
                isDirectory: FfiConverterBool.read(from: &buf), 
                size: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: RemoteFileRecord, into buf: inout [UInt8]) {
        FfiConverterString.write(value.name, into: &buf)
        FfiConverterString.write(value.path, into: &buf)
        FfiConverterBool.write(value.isDirectory, into: &buf)
        FfiConverterUInt64.write(value.size, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRemoteFileRecord_lift(_ buf: RustBuffer) throws -> RemoteFileRecord {
    return try FfiConverterTypeRemoteFileRecord.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRemoteFileRecord_lower(_ value: RemoteFileRecord) -> RustBuffer {
    return FfiConverterTypeRemoteFileRecord.lower(value)
}


/**
 * A queued or completed file transfer operation.
 */
public struct TransferTaskRecord: Equatable, Hashable {
    public var id: UInt64
    public var direction: TransferDirectionRecord
    public var localPath: String
    public var remotePath: String
    public var status: TransferStatusRecord

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(id: UInt64, direction: TransferDirectionRecord, localPath: String, remotePath: String, status: TransferStatusRecord) {
        self.id = id
        self.direction = direction
        self.localPath = localPath
        self.remotePath = remotePath
        self.status = status
    }

    

    
}

#if compiler(>=6)
extension TransferTaskRecord: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransferTaskRecord: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransferTaskRecord {
        return
            try TransferTaskRecord(
                id: FfiConverterUInt64.read(from: &buf), 
                direction: FfiConverterTypeTransferDirectionRecord.read(from: &buf), 
                localPath: FfiConverterString.read(from: &buf), 
                remotePath: FfiConverterString.read(from: &buf), 
                status: FfiConverterTypeTransferStatusRecord.read(from: &buf)
        )
    }

    public static func write(_ value: TransferTaskRecord, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.id, into: &buf)
        FfiConverterTypeTransferDirectionRecord.write(value.direction, into: &buf)
        FfiConverterString.write(value.localPath, into: &buf)
        FfiConverterString.write(value.remotePath, into: &buf)
        FfiConverterTypeTransferStatusRecord.write(value.status, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferTaskRecord_lift(_ buf: RustBuffer) throws -> TransferTaskRecord {
    return try FfiConverterTypeTransferTaskRecord.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferTaskRecord_lower(_ value: TransferTaskRecord) -> RustBuffer {
    return FfiConverterTypeTransferTaskRecord.lower(value)
}

// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Authentication method for a connection profile.
 */

public enum AuthTypeRecord: Equatable, Hashable {
    
    case password(password: String
    )
    case privateKey(keyPath: String, passphrase: String?
    )





}

#if compiler(>=6)
extension AuthTypeRecord: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAuthTypeRecord: FfiConverterRustBuffer {
    typealias SwiftType = AuthTypeRecord

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AuthTypeRecord {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .password(password: try FfiConverterString.read(from: &buf)
        )
        
        case 2: return .privateKey(keyPath: try FfiConverterString.read(from: &buf), passphrase: try FfiConverterOptionString.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: AuthTypeRecord, into buf: inout [UInt8]) {
        switch value {
        
        
        case let .password(password):
            writeInt(&buf, Int32(1))
            FfiConverterString.write(password, into: &buf)
            
        
        case let .privateKey(keyPath,passphrase):
            writeInt(&buf, Int32(2))
            FfiConverterString.write(keyPath, into: &buf)
            FfiConverterOptionString.write(passphrase, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuthTypeRecord_lift(_ buf: RustBuffer) throws -> AuthTypeRecord {
    return try FfiConverterTypeAuthTypeRecord.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuthTypeRecord_lower(_ value: AuthTypeRecord) -> RustBuffer {
    return FfiConverterTypeAuthTypeRecord.lower(value)
}



/**
 * Flat error type exposed to Swift.
 */
public enum DockBridgeError: Swift.Error, Equatable, Hashable, Foundation.LocalizedError {

    
    
    case Generic(message: String)
    

    

    

    
    public var errorDescription: String? {
        String(reflecting: self)
    }
    
}

#if compiler(>=6)
extension DockBridgeError: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDockBridgeError: FfiConverterRustBuffer {
    typealias SwiftType = DockBridgeError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DockBridgeError {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        

        
        case 1: return .Generic(
            message: try FfiConverterString.read(from: &buf)
        )
        

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: DockBridgeError, into buf: inout [UInt8]) {
        switch value {

        

        
        case .Generic(_ /* message is ignored*/):
            writeInt(&buf, Int32(1))

        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDockBridgeError_lift(_ buf: RustBuffer) throws -> DockBridgeError {
    return try FfiConverterTypeDockBridgeError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDockBridgeError_lower(_ value: DockBridgeError) -> RustBuffer {
    return FfiConverterTypeDockBridgeError.lower(value)
}

// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Direction of a file transfer task.
 */

public enum TransferDirectionRecord: Equatable, Hashable {
    
    case upload
    case download





}

#if compiler(>=6)
extension TransferDirectionRecord: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransferDirectionRecord: FfiConverterRustBuffer {
    typealias SwiftType = TransferDirectionRecord

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransferDirectionRecord {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .upload
        
        case 2: return .download
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: TransferDirectionRecord, into buf: inout [UInt8]) {
        switch value {
        
        
        case .upload:
            writeInt(&buf, Int32(1))
        
        
        case .download:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferDirectionRecord_lift(_ buf: RustBuffer) throws -> TransferDirectionRecord {
    return try FfiConverterTypeTransferDirectionRecord.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferDirectionRecord_lower(_ value: TransferDirectionRecord) -> RustBuffer {
    return FfiConverterTypeTransferDirectionRecord.lower(value)
}


// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Lifecycle status of a transfer task.
 */

public enum TransferStatusRecord: Equatable, Hashable {
    
    case pending
    case inProgress
    case completed
    case failed(message: String
    )
    case cancelled





}

#if compiler(>=6)
extension TransferStatusRecord: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransferStatusRecord: FfiConverterRustBuffer {
    typealias SwiftType = TransferStatusRecord

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransferStatusRecord {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .pending
        
        case 2: return .inProgress
        
        case 3: return .completed
        
        case 4: return .failed(message: try FfiConverterString.read(from: &buf)
        )
        
        case 5: return .cancelled
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: TransferStatusRecord, into buf: inout [UInt8]) {
        switch value {
        
        
        case .pending:
            writeInt(&buf, Int32(1))
        
        
        case .inProgress:
            writeInt(&buf, Int32(2))
        
        
        case .completed:
            writeInt(&buf, Int32(3))
        
        
        case let .failed(message):
            writeInt(&buf, Int32(4))
            FfiConverterString.write(message, into: &buf)
            
        
        case .cancelled:
            writeInt(&buf, Int32(5))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferStatusRecord_lift(_ buf: RustBuffer) throws -> TransferStatusRecord {
    return try FfiConverterTypeTransferStatusRecord.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferStatusRecord_lower(_ value: TransferStatusRecord) -> RustBuffer {
    return FfiConverterTypeTransferStatusRecord.lower(value)
}





/**
 * Callback invoked when an active SSH/SFTP session is lost.
 */
public protocol ConnectionEventHandler: AnyObject, Sendable {
    
    func onSessionDisconnected(sessionId: UInt64, reason: String) 
    
}


// Put the implementation in a struct so we don't pollute the top-level namespace
fileprivate struct UniffiCallbackInterfaceConnectionEventHandler {

    // Create the VTable using a series of closures.
    // Swift automatically converts these into C callback functions.
    //
    // Store the vtable directly.
    static let vtable: UniffiVTableCallbackInterfaceConnectionEventHandler = UniffiVTableCallbackInterfaceConnectionEventHandler(
        uniffiFree: { (uniffiHandle: UInt64) -> () in
            do {
                try FfiConverterCallbackInterfaceConnectionEventHandler.handleMap.remove(handle: uniffiHandle)
            } catch {
                print("Uniffi callback interface ConnectionEventHandler: handle missing in uniffiFree")
            }
        },
        uniffiClone: { (uniffiHandle: UInt64) -> UInt64 in
            do {
                return try FfiConverterCallbackInterfaceConnectionEventHandler.handleMap.clone(handle: uniffiHandle)
            } catch {
                fatalError("Uniffi callback interface ConnectionEventHandler: handle missing in uniffiClone")
            }
        },
        onSessionDisconnected: { (
            uniffiHandle: UInt64,
            sessionId: UInt64,
            reason: RustBuffer,
            uniffiOutReturn: UnsafeMutableRawPointer,
            uniffiCallStatus: UnsafeMutablePointer<RustCallStatus>
        ) in
            let makeCall = {
                () throws -> () in
                guard let uniffiObj = try? FfiConverterCallbackInterfaceConnectionEventHandler.handleMap.get(handle: uniffiHandle) else {
                    throw UniffiInternalError.unexpectedStaleHandle
                }
                return uniffiObj.onSessionDisconnected(
                     sessionId: try FfiConverterUInt64.lift(sessionId),
                     reason: try FfiConverterString.lift(reason)
                )
            }

            
            let writeReturn = { () }
            uniffiTraitInterfaceCall(
                callStatus: uniffiCallStatus,
                makeCall: makeCall,
                writeReturn: writeReturn
            )
        }
    )

    // Rust stores this pointer for future callback invocations, so it must live
    // for the process lifetime (not just for the init function call).
    static let vtablePtr: UnsafePointer<UniffiVTableCallbackInterfaceConnectionEventHandler> = {
        let ptr = UnsafeMutablePointer<UniffiVTableCallbackInterfaceConnectionEventHandler>.allocate(capacity: 1)
        ptr.initialize(to: vtable)
        return UnsafePointer(ptr)
    }()
}

private func uniffiCallbackInitConnectionEventHandler() {
    uniffi_dockbridge_uniffi_fn_init_callback_vtable_connectioneventhandler(UniffiCallbackInterfaceConnectionEventHandler.vtablePtr)
}

// FfiConverter protocol for callback interfaces
#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterCallbackInterfaceConnectionEventHandler {
    fileprivate static let handleMap = UniffiHandleMap<ConnectionEventHandler>()
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
extension FfiConverterCallbackInterfaceConnectionEventHandler : FfiConverter {
    typealias SwiftType = ConnectionEventHandler
    typealias FfiType = UInt64

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ handle: UInt64) throws -> SwiftType {
        try handleMap.get(handle: handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        let handle: UInt64 = try readInt(&buf)
        return try lift(handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ v: SwiftType) -> UInt64 {
        return handleMap.insert(obj: v)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func write(_ v: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(v))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfaceConnectionEventHandler_lift(_ handle: UInt64) throws -> ConnectionEventHandler {
    return try FfiConverterCallbackInterfaceConnectionEventHandler.lift(handle)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfaceConnectionEventHandler_lower(_ v: ConnectionEventHandler) -> UInt64 {
    return FfiConverterCallbackInterfaceConnectionEventHandler.lower(v)
}




/**
 * Callback invoked when a host key is not yet trusted.
 */
public protocol HostKeyHandler: AnyObject, Sendable {
    
    func promptUnknownHost(challenge: HostKeyChallenge)  -> Bool
    
}


// Put the implementation in a struct so we don't pollute the top-level namespace
fileprivate struct UniffiCallbackInterfaceHostKeyHandler {

    // Create the VTable using a series of closures.
    // Swift automatically converts these into C callback functions.
    //
    // Store the vtable directly.
    static let vtable: UniffiVTableCallbackInterfaceHostKeyHandler = UniffiVTableCallbackInterfaceHostKeyHandler(
        uniffiFree: { (uniffiHandle: UInt64) -> () in
            do {
                try FfiConverterCallbackInterfaceHostKeyHandler.handleMap.remove(handle: uniffiHandle)
            } catch {
                print("Uniffi callback interface HostKeyHandler: handle missing in uniffiFree")
            }
        },
        uniffiClone: { (uniffiHandle: UInt64) -> UInt64 in
            do {
                return try FfiConverterCallbackInterfaceHostKeyHandler.handleMap.clone(handle: uniffiHandle)
            } catch {
                fatalError("Uniffi callback interface HostKeyHandler: handle missing in uniffiClone")
            }
        },
        promptUnknownHost: { (
            uniffiHandle: UInt64,
            challenge: RustBuffer,
            uniffiOutReturn: UnsafeMutablePointer<Int8>,
            uniffiCallStatus: UnsafeMutablePointer<RustCallStatus>
        ) in
            let makeCall = {
                () throws -> Bool in
                guard let uniffiObj = try? FfiConverterCallbackInterfaceHostKeyHandler.handleMap.get(handle: uniffiHandle) else {
                    throw UniffiInternalError.unexpectedStaleHandle
                }
                return uniffiObj.promptUnknownHost(
                     challenge: try FfiConverterTypeHostKeyChallenge_lift(challenge)
                )
            }

            
            let writeReturn = { uniffiOutReturn.pointee = FfiConverterBool.lower($0) }
            uniffiTraitInterfaceCall(
                callStatus: uniffiCallStatus,
                makeCall: makeCall,
                writeReturn: writeReturn
            )
        }
    )

    // Rust stores this pointer for future callback invocations, so it must live
    // for the process lifetime (not just for the init function call).
    static let vtablePtr: UnsafePointer<UniffiVTableCallbackInterfaceHostKeyHandler> = {
        let ptr = UnsafeMutablePointer<UniffiVTableCallbackInterfaceHostKeyHandler>.allocate(capacity: 1)
        ptr.initialize(to: vtable)
        return UnsafePointer(ptr)
    }()
}

private func uniffiCallbackInitHostKeyHandler() {
    uniffi_dockbridge_uniffi_fn_init_callback_vtable_hostkeyhandler(UniffiCallbackInterfaceHostKeyHandler.vtablePtr)
}

// FfiConverter protocol for callback interfaces
#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterCallbackInterfaceHostKeyHandler {
    fileprivate static let handleMap = UniffiHandleMap<HostKeyHandler>()
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
extension FfiConverterCallbackInterfaceHostKeyHandler : FfiConverter {
    typealias SwiftType = HostKeyHandler
    typealias FfiType = UInt64

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ handle: UInt64) throws -> SwiftType {
        try handleMap.get(handle: handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        let handle: UInt64 = try readInt(&buf)
        return try lift(handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ v: SwiftType) -> UInt64 {
        return handleMap.insert(obj: v)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func write(_ v: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(v))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfaceHostKeyHandler_lift(_ handle: UInt64) throws -> HostKeyHandler {
    return try FfiConverterCallbackInterfaceHostKeyHandler.lift(handle)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfaceHostKeyHandler_lower(_ v: HostKeyHandler) -> UInt64 {
    return FfiConverterCallbackInterfaceHostKeyHandler.lower(v)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionString: FfiConverterRustBuffer {
    typealias SwiftType = String?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeRemoteFileRecord: FfiConverterRustBuffer {
    typealias SwiftType = [RemoteFileRecord]

    public static func write(_ value: [RemoteFileRecord], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeRemoteFileRecord.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [RemoteFileRecord] {
        let len: Int32 = try readInt(&buf)
        var seq = [RemoteFileRecord]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeRemoteFileRecord.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeTransferTaskRecord: FfiConverterRustBuffer {
    typealias SwiftType = [TransferTaskRecord]

    public static func write(_ value: [TransferTaskRecord], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeTransferTaskRecord.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [TransferTaskRecord] {
        let len: Int32 = try readInt(&buf)
        var seq = [TransferTaskRecord]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeTransferTaskRecord.read(from: &buf))
        }
        return seq
    }
}

private enum InitializationResult {
    case ok
    case contractVersionMismatch
    case apiChecksumMismatch
}
// Use a global variable to perform the versioning checks. Swift ensures that
// the code inside is only computed once.
private let initializationResult: InitializationResult = {
    // Get the bindings contract version from our ComponentInterface
    let bindings_contract_version = 30
    // Get the scaffolding contract version by calling the into the dylib
    let scaffolding_contract_version = ffi_dockbridge_uniffi_uniffi_contract_version()
    if bindings_contract_version != scaffolding_contract_version {
        return InitializationResult.contractVersionMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_cancel_transfer() != 17692) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_connect() != 3874) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_create_directory() != 61517) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_delete() != 53272) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_disconnect() != 3529) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_download() != 58889) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_download_entry() != 36929) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_get_initial_directory() != 10511) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_get_transfer_queue() != 62228) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_list_directory() != 36315) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_rename() != 27195) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_upload() != 54637) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_dockbridgeclient_upload_entry() != 14643) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_constructor_dockbridgeclient_new() != 5215) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_connectioneventhandler_on_session_disconnected() != 54402) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_dockbridge_uniffi_checksum_method_hostkeyhandler_prompt_unknown_host() != 7522) {
        return InitializationResult.apiChecksumMismatch
    }

    uniffiCallbackInitConnectionEventHandler()
    uniffiCallbackInitHostKeyHandler()
    return InitializationResult.ok
}()

// Make the ensure init function public so that other modules which have external type references to
// our types can call it.
public func uniffiEnsureDockbridgeUniffiInitialized() {
    switch initializationResult {
    case .ok:
        break
    case .contractVersionMismatch:
        fatalError("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    case .apiChecksumMismatch:
        fatalError("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

// swiftlint:enable all