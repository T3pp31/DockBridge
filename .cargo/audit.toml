# Tracked exceptions for known advisories with no immediate fix.
# See docs/security.md for remediation plans and the response workflow.

[advisories]
ignore = [
    "RUSTSEC-2023-0071", # rsa: Marvin Attack — no fixed upgrade available upstream
    "RUSTSEC-2026-0153", # russh-cryptovec — fixed in russh >=0.60.3 (blocked on Rust 1.85+)
    "RUSTSEC-2026-0154", # russh — fixed in russh >=0.60.3 (blocked on Rust 1.85+)
]
